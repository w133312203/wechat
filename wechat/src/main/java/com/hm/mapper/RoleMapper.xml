<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hm.dao.RoleDao">
	<select id="find" parameterType="java.lang.Integer" resultType="Role">
    	select id,rolename,description,type,moneys from hui_role where id=#{id}
    </select>
</mapper>